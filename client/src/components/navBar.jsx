import { styles } from "../styles";

const NavBar = () => {
  return (
    <div className={`top-0 z-20 backdrop-blur sticky bg-red-600`}>
      {/* <div className={`app-bar flex flex-row justify-evenly items-center text-white ${styles.paddingX}`}>
        <p className="font-bold text-[25px]">TXTME</p>
        <p>Norhane</p>
      </div> */}
    </div>
  );
};

export default NavBar;
